{
  "name": "codelab-bootstrap",
  "version": "1.0.0",
  "description": "use nodemon to watch changes on md file, rebuild codelab on saves",
  "main": "index.js",
  "scripts": {
    "watch": "nodemon --watch ep-discovery-kafka.md --exec 'claat export ep-discovery-kafka.md && lsof -c claat -t | xargs kill && cd ep-discovery-kafka && claat serve'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/SolaceDev/solace-dev-codelabs/"
  },
  "keywords": [
    "codelab"
  ],
  "dependencies": {
    "nodemon": "^2.0.3"
  }
}
