{
  "name": "codelab-bootstrap",
  "version": "1.0.0",
  "description": "use nodemon to watch changes on md file, rebuild codelab on saves",
  "main": "index.js",
  "scripts": {
    "watch": "nodemon --watch opentelemetry-intro.md --exec 'claat export opentelemetry-intro.md && lsof -c claat -t | xargs kill && cd opentelemetry-intro && claat serve'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/SolaceDev/solace-dev-codelabs/"
  },
  "keywords": [
    "codelab"
  ],
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.3"
  }
}
