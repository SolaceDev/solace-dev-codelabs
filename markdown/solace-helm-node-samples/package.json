{
  "name": "codelab-bootstrap",
  "version": "1.0.0",
  "description": "use nodemon to watch changes on md file, rebuild codelab on saves",
  "main": "index.js",
  "scripts": {
    "watch": "nodemon --watch helm-node-samples.md --exec 'claat export helm-node-samples.md && lsof -c claat -t | xargs kill && cd helm-node-samples && claat serve'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/SolaceDev/solace-dev-codelabs/tree/master/markdown/helm-node-samples"
  },
  "keywords": [
    "codelab"
  ],
  "dependencies": {
    "nodemon": "^2.0.3"
  }
}
